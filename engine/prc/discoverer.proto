syntax = "proto3";
package prc;
option go_package = "github.com/kercylan98/minotaur/engine/prc";

message DiscovererState {

}

message DiscovererMetadata {
  string rc_physical_address = 1; // 资源控制器物理地址（通常为内网地址）
  map<string, string> user_metadata = 1000; // 用户自定义元数据
}